version: "3.8"

services:
  app:
    build: .
    container_name: blog_api
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - SERVER_HOST=0.0.0.0
      - SERVER_PORT=8080
      - JWT_SECRET=your-secret-key
    restart: unless-stopped
